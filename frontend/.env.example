# 前端环境变量示例
# 部署到 Vercel 时，在项目设置 → Environment Variables 中添加以下变量

# 本地开发用（默认 http://localhost:8000/api，留空即可）
# 生产部署用 Railway 后端地址，例如：
# VITE_API_BASE=https://your-app.up.railway.app/api

VITE_API_BASE=http://localhost:8000/api
